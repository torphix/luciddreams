import 'dart:io';

class LocalStorageAPI{

  // Future<bool> saveImage(File image) async{
    // Checks if directory exists for this app if not creates one
    // Save image file to this directory returning the save location
  // }
}